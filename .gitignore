__pycache__/
*.py[cod]
.DS_Store
.venv/
venv/
.env

# Build output is regenerated by `python3 -m teardown build`.
dist/

# Personal working copy of the capture sheet is fine to commit, but never keys.
*.key
secrets.json
